#My domain name
DOMAIN_NAME=lsabatie.42.fr

#Name for database
DATABASE_NAME=wordpress_db

#SQL admin user
MYSQL_ROOT_USER=louis
MYSQL_ROOT_PASSWORD=louis

#SQL normal user
MYSQL_USER=lsabatie
MYSQL_PASSWORD=lsabatie

DB_HOST=mariadb
USERNAME=lsabatie

WP_TITLE=Inception

#WP admin user
WP_ADMIN=lsabatie
WP_ADMIN_PASSWORD=lsabatie

#WP normal user
WP_USER=wpuser
WP_USER_PASSWORD=wppass

WP_PATH=/var/www/html

# Acceder a la BDD persistante db_volume et wp_volume:

# cd /var/lib
# sudo su
# cd /var/lib/docker/volumes

# lire la BDD en claire :

# depuis le conteneur mariadb :
# mysql -u root -p
# SHOW DATABASES;
# USE wordpress_db;
# SHOW TABLES;
# SELECT * FROM wp_posts LIMIT 10;
